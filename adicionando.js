const notas = [10, 9, 8];

// Se quiser adicionar mais uma valor nesse array (LISTA)
// deve atribuir o valor <push>

notas.push(7)

const media = (notas[0] + notas[1] + notas[2] + notas[3]) / notas.length;

console.log(media)

console.log(notas);